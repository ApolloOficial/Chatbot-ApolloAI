apiVersion: apps/v1
kind: Deployment
metadata:
  name: apolloai
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/environment: homologacao
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: apolloai
          startupProbe:
            httpGet:
              path: /live
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12
